# Optional — API base URL (default /api, proxied to backend in dev)
# VITE_API_URL=http://localhost:5000/api

# Razorpay — publishable key only (same as RAZORPAY_KEY_ID; never put KEY_SECRET here)
# Restart `npm run dev` after changing this file.
VITE_RAZORPAY_KEY_ID=your_key_id_here
